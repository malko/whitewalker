	<dt id="{{test.name}}">
		<span class="name">{{test.name}}</span>
		<div class="actions">{{>test-run-buttons}}</div>
	</dt>
	<dd id="{{test.name}}-reports">
		{{#test.tests as envtest}}{{>test-report}}{{/test.tests}}
	</dd>
